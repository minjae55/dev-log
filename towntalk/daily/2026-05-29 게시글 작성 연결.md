# 2026-05-29 작업 로그

## Today
- Backend: `PostController`, `PostService`, `PostRepository` 구조를 만들고 임시로 게시물 작성이 되도록 했다.
- Frontend: `WritePage` 페이지를 생성하고 메인 페이지와 연결했다.

## Learned
- React에서는 컴포넌트가 요청 객체를 만들고, apiRequest가 실제 HTTP 요청과 토큰 처리를 담당한다.
- DTO 요청은 프론트 JSON key와 백엔드 DTO 필드명이 일치해야 매핑된다.
- permitAll()은 권한 검사만 허용하며 JWT Filter는 별도로 실행될 수 있다.

## Blocker
- 글쓰기 API 인증 처리와 JWT Filter 동작 구간을 추적하는 데 시간이 걸렸다.
- Access Token 전달 흐름이 apiRequest에서 자동 처리된다는 점을 다시 확인했다.

## Next
- Access Token이 sessionStorage에 정상 저장·전달되는지 확인한다.
- /api/post/write 인증 정책을 정리 및 흐름 연결
