# 2026-06-15 작업 로그

## Today

- 사용자의 요청 없이도 시간이 지나면 처리해야 하는 주문 상태를 정리했다.
- 배송 완료 후 자동 거래 확정, 장시간 결제 대기 주문 취소, 협의 응답 기한 확인 등에 Spring `@Scheduled`를 적용할 수 있는지 공부했다.
- `fixedRate`, `fixedDelay`, `cron`의 실행 기준을 비교하고 프로젝트에는 주기 조회 방식이 적합하다고 판단했다.
- 스케줄러 실행 시 중복 처리, 트랜잭션 범위, 대상 조회 조건을 함께 검토했다.

## Learned

- 스케줄러는 사용자의 API 호출이 없어도 정해진 시간이나 주기마다 서버가 비즈니스 로직을 실행하는 기능이다.
- 자동화 로직은 같은 작업이 여러 번 실행돼도 결과가 달라지지 않도록 멱등성을 보장해야 한다.
- 상태와 기준 시각을 함께 조건으로 조회해야 이미 처리된 데이터를 다시 변경하는 문제를 줄일 수 있다.
- 여러 서버 인스턴스에서 같은 스케줄이 실행될 수 있으므로 운영 환경에서는 락이나 전용 배치 구조도 고려해야 한다.

## Pending

- 자동 거래 확정과 견적 만료의 정확한 기준 시간은 팀 정책 협의가 필요하다.
- 현재 프로젝트 규모에서는 `@Scheduled`로 시작하되 다중 인스턴스 대응은 이후 확장 범위로 남겨두었다.

## Next

- 자동 상태 변경 대상과 실행 주기를 표로 정리한다.
- 상태 조건을 포함한 멱등한 서비스 메서드를 먼저 구현한다.
- 스케줄러가 실행되지 않거나 중복 실행되는 상황을 테스트한다.